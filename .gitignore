# deps
node_modules/
.pnpm-store/

# next
.next/
out/
next-env.d.ts

# build
dist/
*.tsbuildinfo

# env — never commit keys
.env
.env.local
.env*.local

# keystore password for the testnet deploy wallet. The KEY itself lives encrypted
# in ~/.foundry/keystores and is never in this repo — this only unlocks it.
.deployer-password

# misc
.DS_Store
*.log
.vercel
