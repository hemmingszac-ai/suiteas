[profile.default]
src = "src"
test = "test"
out = "out"
libs = ["node_modules", "lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
remappings = [
    "@openzeppelin/contracts/=node_modules/@openzeppelin/contracts/",
    "forge-std/=lib/forge-std/src/",
]

# Fuji deploy target. RPC + keys come from the environment, never hardcoded.
[rpc_endpoints]
fuji = "${FUJI_RPC_URL}"
